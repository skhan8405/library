import {
    Meta,
    Story,
    Props,
    Preview,
    ArgsTable
} from "@storybook/addon-docs/blocks";
import { WithKnobs, text, array, object } from "@storybook/addon-knobs";
import Spreadsheet from "../src/index";
import GridComponent from "./index.js";

<Meta title="Spreadsheet Component" component={Spreadsheet} />

# Spreadsheet

## Business component for showing list of data in Spreadsheet

`Spreadsheet` component consists of list of data. Inline edit of cell will be enabled by double clicking the cell's.

### Props

<Props of={Spreadsheet} />

### Code

```jsx
import { Spreadsheet } from "@neo-ui/spreadsheet";

<Spreadsheet
    rows={data.slice(0, pageSize)}
    dataSet={data}
    pageSize={pageSize}
    count={pageSize}
    columns={columns}
    gridHeight={gridHeight}
    updateCellData={updateCellData}
    selectBulkData={selectBulkData}
    maxLeftPinnedColumn={maxLeftPinnedColumn}
    updatedRows={updatedRows}
    isGlobalSearch={isGlobalSearch}
    isColumnFilter={isColumnFilter}
    isGroupSort={isGroupSort}
    isColumnChooser={isColumnChooser}
    isExportData={isExportData}
/>;
```

### Variants

#### With record count

<Story name="With record count">
    <GridComponent
        isTitle={true}
        isGlobalSearch={false}
        isColumnFilter={false}
        isGroupSort={false}
        isColumnChooser={false}
        isExportData={false}
        isSelectAll={false}
        isHeader={true}
    />
</Story>

```jsx
<Spreadsheet
    isTitle={isTitle}
    rows={data.slice(0, pageSize)}
    dataSet={data}
    pageSize={pageSize}
    count={pageSize}
    columns={columns}
    updateCellData={updateCellData}
    selectBulkData={selectBulkData}
    maxLeftPinnedColumn={maxLeftPinnedColumn}
    updatedRows={updatedRows}
    isGlobalSearch={isGlobalSearch}
    isColumnFilter={isColumnFilter}
    isGroupSort={isGroupSort}
    isColumnChooser={isColumnChooser}
    isExportData={isExportData}
    isSelectAll={isSelectAll}
    isHeader={isHeader}
/>
```

#### With custom height

<Story name="With custom height">
    <GridComponent
        isTitle={false}
        gridHeight={500}
        isGlobalSearch={false}
        isColumnFilter={false}
        isGroupSort={false}
        isColumnChooser={false}
        isExportData={false}
        isSelectAll={false}
    />
</Story>

```jsx
<Spreadsheet
    rows={data.slice(0, pageSize)}
    dataSet={data}
    pageSize={pageSize}
    count={pageSize}
    columns={columns}
    gridHeight={gridHeight}
    updateCellData={updateCellData}
    selectBulkData={selectBulkData}
    maxLeftPinnedColumn={maxLeftPinnedColumn}
    updatedRows={updatedRows}
    isTitle={isTitle}
    isGlobalSearch={isGlobalSearch}
    isColumnFilter={isColumnFilter}
    isGroupSort={isGroupSort}
    isColumnChooser={isColumnChooser}
    isExportData={isExportData}
    isSelectAll={isSelectAll}
/>
```

#### With global search

<Story name="With global search">
    <GridComponent
        isTitle={false}
        isGlobalSearch={true}
        isColumnFilter={false}
        isGroupSort={false}
        isColumnChooser={false}
        isExportData={false}
        isSelectAll={false}
        isHeader={true}
    />
</Story>

```jsx
<Spreadsheet
    rows={data.slice(0, pageSize)}
    dataSet={data}
    pageSize={pageSize}
    count={pageSize}
    columns={columns}
    updateCellData={updateCellData}
    selectBulkData={selectBulkData}
    maxLeftPinnedColumn={maxLeftPinnedColumn}
    updatedRows={updatedRows}
    isTitle={isTitle}
    isGlobalSearch={isGlobalSearch}
    isColumnFilter={isColumnFilter}
    isGroupSort={isGroupSort}
    isColumnChooser={isColumnChooser}
    isExportData={isExportData}
    isSelectAll={isSelectAll}
    isHeader={isHeader}
/>
```

#### With column filter

<Story name="With column filter">
    <GridComponent
        isTitle={false}
        isGlobalSearch={false}
        isColumnFilter={true}
        columnFilterStyle={"txt-wrap-single"}
        isGroupSort={false}
        isColumnChooser={false}
        isExportData={false}
        isSelectAll={false}
        isHeader={true}
    />
</Story>

```jsx
<Spreadsheet
    rows={data.slice(0, pageSize)}
    dataSet={data}
    pageSize={pageSize}
    count={pageSize}
    columns={columns}
    updateCellData={updateCellData}
    selectBulkData={selectBulkData}
    maxLeftPinnedColumn={maxLeftPinnedColumn}
    updatedRows={updatedRows}
    isTitle={isTitle}
    isGlobalSearch={isGlobalSearch}
    isColumnFilter={isColumnFilter}
    columnFilterStyle={"columnFilterStyle"}
    isGroupSort={isGroupSort}
    isColumnChooser={isColumnChooser}
    isExportData={isExportData}
    isSelectAll={isSelectAll}
    isHeader={isHeader}
/>
```

#### With group sort

<Story name="With group sort">
    <GridComponent
        isTitle={false}
        isGlobalSearch={false}
        isColumnFilter={false}
        isGroupSort={true}
        isColumnChooser={false}
        isExportData={false}
        isSelectAll={false}
        isHeader={true}
    />
</Story>

```jsx
<Spreadsheet
    rows={data.slice(0, pageSize)}
    dataSet={data}
    pageSize={pageSize}
    count={pageSize}
    columns={columns}
    updateCellData={updateCellData}
    selectBulkData={selectBulkData}
    maxLeftPinnedColumn={maxLeftPinnedColumn}
    updatedRows={updatedRows}
    isTitle={isTitle}
    isGlobalSearch={isGlobalSearch}
    isColumnFilter={isColumnFilter}
    isGroupSort={isGroupSort}
    isColumnChooser={isColumnChooser}
    isExportData={isExportData}
    isSelectAll={isSelectAll}
    isHeader={isHeader}
/>
```

#### With column chooser

<Story name="With column chooser">
    <GridComponent
        isTitle={false}
        isGlobalSearch={false}
        isColumnFilter={false}
        isGroupSort={false}
        isColumnChooser={true}
        isExportData={false}
        isSelectAll={false}
        isHeader={true}
    />
</Story>

```jsx
<Spreadsheet
    rows={data.slice(0, pageSize)}
    dataSet={data}
    pageSize={pageSize}
    count={pageSize}
    columns={columns}
    updateCellData={updateCellData}
    selectBulkData={selectBulkData}
    maxLeftPinnedColumn={maxLeftPinnedColumn}
    updatedRows={updatedRows}
    isTitle={isTitle}
    isGlobalSearch={isGlobalSearch}
    isColumnFilter={isColumnFilter}
    isGroupSort={isGroupSort}
    isColumnChooser={isColumnChooser}
    isExportData={isExportData}
    isSelectAll={isSelectAll}
    isHeader={isHeader}
/>
```

#### With export data

<Story name="With export data">
    <GridComponent
        isTitle={false}
        isGlobalSearch={false}
        isColumnFilter={false}
        isGroupSort={false}
        isColumnChooser={false}
        isExportData={true}
        isSelectAll={false}
        isHeader={true}
    />
</Story>

```jsx
<Spreadsheet
    rows={data.slice(0, pageSize)}
    dataSet={data}
    pageSize={pageSize}
    count={pageSize}
    columns={columns}
    updateCellData={updateCellData}
    selectBulkData={selectBulkData}
    maxLeftPinnedColumn={maxLeftPinnedColumn}
    updatedRows={updatedRows}
    isTitle={isTitle}
    isGlobalSearch={isGlobalSearch}
    isColumnFilter={isColumnFilter}
    isGroupSort={isGroupSort}
    isColumnChooser={isColumnChooser}
    isExportData={isExportData}
    isSelectAll={isSelectAll}
    isHeader={isHeader}
/>
```

#### With select all

<Story name="With select all">
    <GridComponent
        isTitle={false}
        isGlobalSearch={false}
        isColumnFilter={false}
        isGroupSort={false}
        isColumnChooser={false}
        isExportData={false}
        isSelectAll={true}
    />
</Story>

```jsx
<Spreadsheet
    rows={data.slice(0, pageSize)}
    dataSet={data}
    pageSize={pageSize}
    count={pageSize}
    columns={columns}
    updateCellData={updateCellData}
    selectBulkData={selectBulkData}
    maxLeftPinnedColumn={maxLeftPinnedColumn}
    updatedRows={updatedRows}
    isTitle={isTitle}
    isGlobalSearch={isGlobalSearch}
    isColumnFilter={isColumnFilter}
    isGroupSort={isGroupSort}
    isColumnChooser={isColumnChooser}
    isExportData={isExportData}
    isSelectAll={isSelectAll}
/>
```

#### Without header

<Story name="Without header">
    <GridComponent
        isTitle={false}
        isGlobalSearch={false}
        isColumnFilter={false}
        isGroupSort={false}
        isColumnChooser={false}
        isExportData={false}
        isSelectAll={false}
        isHeader={false}
    />
</Story>

```jsx
<Spreadsheet
    rows={data.slice(0, pageSize)}
    dataSet={data}
    pageSize={pageSize}
    count={pageSize}
    columns={columns}
    updateCellData={updateCellData}
    selectBulkData={selectBulkData}
    maxLeftPinnedColumn={maxLeftPinnedColumn}
    updatedRows={updatedRows}
    isTitle={isTitle}
    isGlobalSearch={isGlobalSearch}
    isColumnFilter={isColumnFilter}
    isGroupSort={isGroupSort}
    isColumnChooser={isColumnChooser}
    isExportData={isExportData}
    isSelectAll={isSelectAll}
    isHeader={isHeader}
/>
```

#### With classname

<Story name="With classname">
    <GridComponent
        isTitle={false}
        isGlobalSearch={false}
        isColumnFilter={false}
        isGroupSort={false}
        isColumnChooser={false}
        isExportData={false}
        isSelectAll={false}
        isHeader={false}
        noBorderClassName={"grid-no-border"}
    />
</Story>

```jsx
<Spreadsheet
    rows={data.slice(0, pageSize)}
    dataSet={data}
    pageSize={pageSize}
    count={pageSize}
    columns={columns}
    updateCellData={updateCellData}
    selectBulkData={selectBulkData}
    maxLeftPinnedColumn={maxLeftPinnedColumn}
    updatedRows={updatedRows}
    isTitle={isTitle}
    isGlobalSearch={isGlobalSearch}
    isColumnFilter={isColumnFilter}
    isGroupSort={isGroupSort}
    isColumnChooser={isColumnChooser}
    isExportData={isExportData}
    isSelectAll={isSelectAll}
    isHeader={isHeader}
    noBorderClassName={noBorderClassName}
/>
```

#### With all props

<Story name="With all props">
    <GridComponent
        isTitle={true}
        isTitle={true}
        gridHeight={460}
        isGlobalSearch={true}
        isColumnFilter={true}
        columnFilterStyle={"txt-wrap"}
        isGroupSort={true}
        isColumnChooser={true}
        isExportData={true}
        isSelectAll={true}
        isHeader={true}
        noBorderClassName={"grid-no-border"}
    />
</Story>

```jsx
<Spreadsheet
    isTitle={isTitle}
    rows={data.slice(0, pageSize)}
    dataSet={data}
    pageSize={pageSize}
    count={pageSize}
    columns={columns}
    gridHeight={gridHeight}
    updateCellData={updateCellData}
    selectBulkData={selectBulkData}
    maxLeftPinnedColumn={maxLeftPinnedColumn}
    updatedRows={updatedRows}
    isGlobalSearch={isGlobalSearch}
    isColumnFilter={isColumnFilter}
    columnFilterStyle={"columnFilterStyle"}
    isGroupSort={isGroupSort}
    isColumnChooser={isColumnChooser}
    isExportData={isExportData}
    isSelectAll={isSelectAll}
    isHeader={isHeader}
    noBorderClassName={noBorderClassName}
/>
```
